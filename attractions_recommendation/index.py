from llama_index.core import VectorStoreIndex

# to build an index, you need to first load the data and create a vector store
# from the vector store, you can create an index
# you can reload and update the index by updating the vector store with new data and rebuilding the index
